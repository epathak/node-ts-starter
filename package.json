{
  "name": "node-ts-template",
  "version": "1.0.0",
  "description": "Minimalist starter template for node with typescript!",
  "main": "server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node ./dist/server.js",
    "debug": "rimraf ./dist && npm run build && node --inspect ./dist/server.js"
  },
  "author": "Tejas Pathak",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.3.3",
    "rimraf": "^3.0.2",
    "sonar-scanner": "^3.1.0",
    "typescript": "^4.3.5"
  }
}
